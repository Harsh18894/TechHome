<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techHome">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.EnterModeActivity" />

        <activity
            android:name=".activities.LoginActivity"
            android:windowSoftInputMode="adjustPan|adjustResize" />

        <activity
            android:name=".activities.RegisterActivity"
            android:windowSoftInputMode="adjustPan|adjustResize">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.EnterModeActivity" />
        </activity>

        <activity android:name=".activities.DashboardActivity" />
        <activity android:name=".activities.AppliancesActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.DashboardActivity" />
        </activity>
        <activity android:name=".activities.ElectricalActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.DashboardActivity" />
        </activity>
        <activity android:name=".activities.WiringActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.DashboardActivity" />
        </activity>
        <activity android:name=".activities.PlumbingActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.DashboardActivity" />
        </activity>

    </application>

</manifest>
